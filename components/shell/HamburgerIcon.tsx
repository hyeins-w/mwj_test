/** 기존 index.html(중첩 div)과 나머지 페이지(단순 div 3개)로 갈라져 있던 햄버거 아이콘을
 *  실제 렌더 결과가 같은 쪽(단순 div 3개)으로 통일한 것. */
export function HamburgerIcon() {
  return (
    <>
      <div />
      <div />
      <div />
    </>
  );
}
